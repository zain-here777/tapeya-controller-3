import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition NotOut. */
const fstNotOutSample = {
  ...fstMatchBase,
  actionLabel: "NOT OUT",
};

export default fstNotOutSample;
