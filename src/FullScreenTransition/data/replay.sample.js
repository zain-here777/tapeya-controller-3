import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition Replay. */
const fstReplaySample = {
  ...fstMatchBase,
  actionLabel: "REPLAY",
};

export default fstReplaySample;
