import { createFullScreenConfig } from "./shared.config.js";

/** NeedTarget — full-screen chase / target graphic. */
export const needTargetFsConfig = createFullScreenConfig({
  id: "need-target",
  title: "NEED TARGET",
});
