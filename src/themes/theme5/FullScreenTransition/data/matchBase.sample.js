import { createScorecardFeed } from "../../data/matchIdentity.js";

/**
 * Shared scorecard feed for Full Screen Transition samples.
 * Each sample spreads this and sets `actionLabel`.
 */
export const fstMatchBase = createScorecardFeed();
