import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition DECISION PENDING. */
const fstDecisionPendingSample = {
  ...fstMatchBase,
  actionLabel: "DECISION PENDING",
};

export default fstDecisionPendingSample;
