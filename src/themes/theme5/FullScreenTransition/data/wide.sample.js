import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition WIDE. */
const fstWideSample = {
  ...fstMatchBase,
  actionLabel: "WIDE",
};

export default fstWideSample;
