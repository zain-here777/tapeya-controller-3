import { createScorecardFeed } from "../../data/matchIdentity.js";

/**
 * Shared ThisOver-style match feed for Theme 5 Tour-Hit samples.
 * Each controller sample spreads this and adds its own `tourHit` badge.
 */
export const tourHitMatchBase = createScorecardFeed();
