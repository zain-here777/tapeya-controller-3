import { createLowerThirdConfig } from "./shared.config.js";

/**
 * LastWicket — isolated config.
 */
export const lastWicketConfig = createLowerThirdConfig({
  id: "last-wicket",
  defaultTeamColorKey: "teamA",
});
