import { createActionBannerConfig } from "./actionBanner.shared.js";

/** No Ball — same broadcast action banner as Four. */
export const noBallConfig = createActionBannerConfig({
  id: "no-ball",
  fallbackLabel: "No Ball",
});
