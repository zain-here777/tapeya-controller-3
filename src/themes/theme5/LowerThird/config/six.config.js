import { createActionBannerConfig } from "./actionBanner.shared.js";

/** Six — same broadcast action banner as Four. */
export const sixConfig = createActionBannerConfig({
  id: "six",
  fallbackLabel: "Six",
  showConfetti: true,
});
