import { createActionBannerConfig } from "./actionBanner.shared.js";

/** Wide — same broadcast action banner as Four. */
export const wideConfig = createActionBannerConfig({
  id: "wide",
  fallbackLabel: "Wide",
});
