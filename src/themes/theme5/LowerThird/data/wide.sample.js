import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 Wide controller only. */
const wideSample = createActionBannerSample("Wide");

export default wideSample;
