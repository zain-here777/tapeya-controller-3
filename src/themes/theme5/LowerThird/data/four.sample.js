import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 Four controller only. */
const fourSample = createActionBannerSample("Four");

export default fourSample;
