import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 Out controller only. */
const outSample = createActionBannerSample("Out");

export default outSample;
