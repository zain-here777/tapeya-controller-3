import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 Six controller only. */
const sixSample = createActionBannerSample("Six");

export default sixSample;
