import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 100 Up controller only. */
const hundredUpSample = createActionBannerSample("100 Up");

export default hundredUpSample;
