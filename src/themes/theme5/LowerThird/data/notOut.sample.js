import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 Not Out controller only. */
const notOutSample = createActionBannerSample("Not Out");

export default notOutSample;
