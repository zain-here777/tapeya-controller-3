import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 No Ball controller only. */
const noBallSample = createActionBannerSample("No Ball");

export default noBallSample;
