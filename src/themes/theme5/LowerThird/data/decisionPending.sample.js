import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 5 Decision Pending controller only. */
const decisionPendingSample = createActionBannerSample("Decision Pending");

export default decisionPendingSample;
