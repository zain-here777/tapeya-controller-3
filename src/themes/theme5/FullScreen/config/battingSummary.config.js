import { createFullScreenConfig } from "./shared.config.js";

/** BattingSummary — full-screen batting card summary. */
export const battingSummaryConfig = createFullScreenConfig({
  id: "batting-summary",
  title: "BATTING SUMMARY",
});
