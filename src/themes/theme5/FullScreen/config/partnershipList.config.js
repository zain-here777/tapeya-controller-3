import { createFullScreenConfig } from "./shared.config.js";

/** PartnershipList — full-screen partnership contribution list. */
export const partnershipListConfig = createFullScreenConfig({
  id: "partnership-list",
  title: "PARTNERSHIP LIST",
});
