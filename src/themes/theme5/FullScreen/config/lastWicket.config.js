import { createFullScreenConfig } from "./shared.config.js";

/** LastWicket — full-screen dismissed batter graphic. */
export const lastWicketFsConfig = createFullScreenConfig({
  id: "last-wicket",
  title: "LAST WICKET",
});
