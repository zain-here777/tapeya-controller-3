import { createIdentityMatch } from "../../data/matchIdentity.js";

/** Sample data for Theme 5 This Match full-screen controller. */
const thisMatchSample = {
  ...createIdentityMatch(),
  statusLabel: "THIS MATCH",
};

export default thisMatchSample;
