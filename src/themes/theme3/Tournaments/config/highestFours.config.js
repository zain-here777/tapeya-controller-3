import { createTournamentConfig } from "./shared.config.js";

/**
 * HighestFours — full-screen tournament fours leaderboard.
 */
export const highestFoursConfig = createTournamentConfig({
  id: "highest-fours",
  title: "HIGHEST FOURS",
});
