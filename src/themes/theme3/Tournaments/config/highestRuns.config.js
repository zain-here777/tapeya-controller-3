import { createTournamentConfig } from "./shared.config.js";

/**
 * HighestRuns — full-screen tournament runs leaderboard.
 */
export const highestRunsConfig = createTournamentConfig({
  id: "highest-runs",
  title: "HIGHEST RUNS",
});
