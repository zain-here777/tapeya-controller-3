import { createTournamentConfig } from "./shared.config.js";

/**
 * HighestSixes — full-screen tournament sixes leaderboard.
 */
export const highestSixesConfig = createTournamentConfig({
  id: "highest-sixes",
  title: "HIGHEST SIXES",
});
