import { createTournamentConfig } from "./shared.config.js";

/**
 * HighestWickets — full-screen tournament wickets leaderboard.
 */
export const highestWicketsConfig = createTournamentConfig({
  id: "highest-wickets",
  title: "HIGHEST WICKETS",
});
