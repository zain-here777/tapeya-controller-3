import { createLowerThirdConfig } from "./shared.config.js";

/**
 * Scorers — isolated config.
 * Same compact bar shell as Umpires / MiniScorecard.
 */
export const scorersConfig = createLowerThirdConfig({
  id: "scorers",
});
