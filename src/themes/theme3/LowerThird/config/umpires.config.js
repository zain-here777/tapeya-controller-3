import { createLowerThirdConfig } from "./shared.config.js";

/**
 * Umpires — isolated config.
 * Same compact bar shell as MiniScorecard.
 */
export const umpiresConfig = createLowerThirdConfig({
  id: "umpires",
});
