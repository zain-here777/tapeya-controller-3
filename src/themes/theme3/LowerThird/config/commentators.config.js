import { createLowerThirdConfig } from "./shared.config.js";

/**
 * Commentators — isolated config.
 * Same compact bar shell as Umpires / Scorers.
 */
export const commentatorsConfig = createLowerThirdConfig({
  id: "commentators",
});
