import { createLowerThirdConfig } from "./shared.config.js";

/**
 * Four — isolated config.
 * Boundary celebration lower third on batsman panel color.
 */
export const fourConfig = createLowerThirdConfig({
  id: "four",
});
