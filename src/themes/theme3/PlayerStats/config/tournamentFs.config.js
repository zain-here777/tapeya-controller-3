import { createPlayerStatsConfig } from "./shared.config.js";

/**
 * TournamentFS — full-screen batsman tournament career graphic.
 * Same UI shell as MatchFS.
 */
export const tournamentFsConfig = createPlayerStatsConfig({
  id: "tournament-fs",
  role: "batsman",
});
