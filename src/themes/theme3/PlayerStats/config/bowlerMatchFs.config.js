import { createPlayerStatsConfig } from "./shared.config.js";

/** Bowler MatchFS config. */
export const bowlerMatchFsConfig = createPlayerStatsConfig({
  id: "bowler-match-fs",
  role: "bowler",
});
