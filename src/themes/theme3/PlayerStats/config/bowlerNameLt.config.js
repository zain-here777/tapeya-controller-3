import { createPlayerStatsConfig } from "./shared.config.js";

/** Bowler NameLT — same shell as batsman NameLT. */
export const bowlerNameLtConfig = createPlayerStatsConfig({
  id: "bowler-name-lt",
  role: "bowler",
});
