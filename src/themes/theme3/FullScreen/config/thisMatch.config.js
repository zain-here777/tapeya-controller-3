import { createFullScreenConfig } from "./shared.config.js";

/**
 * ThisMatch — full-screen matchup graphic.
 * Same UI shell as InningsBreak.
 */
export const thisMatchConfig = createFullScreenConfig({
  id: "this-match",
  statusLabel: "THIS MATCH",
});
