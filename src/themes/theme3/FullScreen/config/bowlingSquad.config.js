import { createFullScreenConfig } from "./shared.config.js";

/** BowlingSquad — full-screen single-team bowling squad grid. */
export const bowlingSquadConfig = createFullScreenConfig({
  id: "bowling-squad",
  title: "BOWLING SQUAD",
});
