import { createFullScreenConfig } from "./shared.config.js";

/** BattingSquad — full-screen single-team batting squad grid. */
export const battingSquadConfig = createFullScreenConfig({
  id: "batting-squad",
  title: "BATTING SQUAD",
});
