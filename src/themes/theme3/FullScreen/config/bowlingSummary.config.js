import { createFullScreenConfig } from "./shared.config.js";

/** BowlingSummary — full-screen bowling card summary. */
export const bowlingSummaryConfig = createFullScreenConfig({
  id: "bowling-summary",
  title: "BOWLING SUMMARY",
});
