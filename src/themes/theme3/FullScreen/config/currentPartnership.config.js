import { createFullScreenConfig } from "./shared.config.js";

/** CurrentPartnership — full-screen current partnership graphic. */
export const currentPartnershipFsConfig = createFullScreenConfig({
  id: "current-partnership",
  title: "CURRENT PARTNERSHIP",
});
