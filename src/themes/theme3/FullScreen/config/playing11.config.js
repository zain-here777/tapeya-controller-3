import { createFullScreenConfig } from "./shared.config.js";

/** Playing11 — full-screen playing XI for both teams. */
export const playing11Config = createFullScreenConfig({
  id: "playing-11",
  title: "PLAYING XI",
});
