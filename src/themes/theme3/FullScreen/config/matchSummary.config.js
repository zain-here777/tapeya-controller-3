import { createFullScreenConfig } from "./shared.config.js";

/** MatchSummary — full-screen dual-innings match summary. */
export const matchSummaryFsConfig = createFullScreenConfig({
  id: "match-summary",
  title: "MATCH SUMMARY",
});
