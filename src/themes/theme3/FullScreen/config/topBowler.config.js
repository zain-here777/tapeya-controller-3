import { createFullScreenConfig } from "./shared.config.js";

/** TopBowler — full-screen top bowler leaderboard. */
export const topBowlerConfig = createFullScreenConfig({
  id: "top-bowler",
  title: "TOP BOWLER",
});
