import { tourHitMatchBase } from "./matchBase.sample.js";

/** Sample match data for Tour-Hit Four only. */
const tourHitFourSample = {
  ...tourHitMatchBase,
  tourHit: {
    eyebrow: "TOURNAMENT",
    title: "FOURS",
    value: 1,
  },
};

export default tourHitFourSample;
