import { createTourHitConfig } from "./shared.config.js";

/** Tour-Hit Fifties — mini badge shows tournament fifties count. */
export const tourHitFiftiesConfig = createTourHitConfig({
  id: "tour-hit-fifties",
  miniScorecard: {
    title: "FIFTIES",
  },
});
