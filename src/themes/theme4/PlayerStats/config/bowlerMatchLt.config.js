import { createPlayerStatsConfig } from "./shared.config.js";

export const bowlerMatchLtConfig = createPlayerStatsConfig({
  id: "bowler-match-lt",
  role: "bowler",
  defaultTeamColorKey: "teamB",
});
