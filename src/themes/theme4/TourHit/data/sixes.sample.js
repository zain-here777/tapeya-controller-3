import { tourHitMatchBase } from "./matchBase.sample.js";

/** Sample match data for Theme 4 Tour-Hit SIXES only. */
const tourHitSixesSample = {
  ...tourHitMatchBase,
  tourHit: {
    eyebrow: "TOURNAMENT",
    title: "SIXES",
    value: 2,
  },
};

export default tourHitSixesSample;
