import { tourHitMatchBase } from "./matchBase.sample.js";

/** Sample match data for Theme 4 Tour-Hit FIFTIES only. */
const tourHitFiftiesSample = {
  ...tourHitMatchBase,
  tourHit: {
    eyebrow: "TOURNAMENT",
    title: "FIFTIES",
    value: 1,
  },
};

export default tourHitFiftiesSample;
