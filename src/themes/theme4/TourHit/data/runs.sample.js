import { tourHitMatchBase } from "./matchBase.sample.js";

/** Sample match data for Theme 4 Tour-Hit RUNS only. */
const tourHitRunsSample = {
  ...tourHitMatchBase,
  tourHit: {
    eyebrow: "TOURNAMENT",
    title: "RUNS",
    value: 156,
  },
};

export default tourHitRunsSample;
