/** Shared Need Target / Innings Figure card dimensions (Theme 3 parity). */

export const NEED_FS_CARD_MIN_H =
  "min-h-[calc(520px*var(--t4-scale))]";

export const NEED_FS_CARD_MAX_W =
  "max-w-[calc(920px*var(--t4-scale))]";

export const NEED_FS_PAGE_MAX_W =
  "max-w-[calc(1480px*var(--t4-scale))]";
