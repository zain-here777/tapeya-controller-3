import { createFullScreenConfig } from "./shared.config.js";

/** InningsFigure — full-screen innings figures graphic. */
export const inningsFigureConfig = createFullScreenConfig({
  id: "innings-figure",
  title: "INNINGS FIGURE",
});
