import { createFullScreenConfig } from "./shared.config.js";

/** TopBatter — full-screen top batter leaderboard. */
export const topBatterConfig = createFullScreenConfig({
  id: "top-batter",
  title: "TOP BATTER",
});
