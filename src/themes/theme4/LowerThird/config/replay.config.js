import { createActionBannerConfig } from "./actionBanner.shared.js";

/** Replay — same broadcast action banner as Four. */
export const replayConfig = createActionBannerConfig({
  id: "replay",
  fallbackLabel: "Replay",
});
