import { createActionBannerConfig } from "./actionBanner.shared.js";

/** Out — same broadcast action banner as Four. */
export const outConfig = createActionBannerConfig({
  id: "out",
  fallbackLabel: "Out",
});
