import { createActionBannerConfig } from "./actionBanner.shared.js";

/** Not Out — same broadcast action banner as Four. */
export const notOutConfig = createActionBannerConfig({
  id: "not-out",
  fallbackLabel: "Not Out",
});
