import { createActionBannerConfig } from "./actionBanner.shared.js";

/** 50 Up — same broadcast action banner as Four. */
export const fiftyUpConfig = createActionBannerConfig({
  id: "50-up",
  fallbackLabel: "50 Up",
});
