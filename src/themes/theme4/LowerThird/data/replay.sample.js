import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 4 Replay controller only. */
const replaySample = createActionBannerSample("Replay");

export default replaySample;
