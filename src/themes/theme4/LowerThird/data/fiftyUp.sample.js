import { createActionBannerSample } from "../config/actionBanner.shared.js";

/** Sample data for the Theme 4 50 Up controller only. */
const fiftyUpSample = createActionBannerSample("50 Up");

export default fiftyUpSample;
