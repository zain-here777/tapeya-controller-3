import { createScorecardFeed } from "../../data/matchIdentity.js";

/** Sample match data for theme 4 Default controller only. */
const defaultSampleMatch = createScorecardFeed();

export default defaultSampleMatch;
