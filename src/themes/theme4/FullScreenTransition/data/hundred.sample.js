import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition 100. */
const fstHundredSample = {
  ...fstMatchBase,
  actionLabel: "100",
};

export default fstHundredSample;
