import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition OUT. */
const fstOutSample = {
  ...fstMatchBase,
  actionLabel: "OUT",
};

export default fstOutSample;
