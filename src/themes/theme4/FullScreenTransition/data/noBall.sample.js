import { fstMatchBase } from "./matchBase.sample.js";

/** Sample match data for Full Screen Transition NO BALL. */
const fstNoBallSample = {
  ...fstMatchBase,
  actionLabel: "NO BALL",
};

export default fstNoBallSample;
