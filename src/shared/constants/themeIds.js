/** Canonical theme identifiers used across the application. */
export const THEME_IDS = {
  THEME_3: "theme3",
  THEME_4: "theme4",
  THEME_5: "theme5",
};

/** Default theme when none is selected or stored. */
export const DEFAULT_THEME_ID = THEME_IDS.THEME_3;
