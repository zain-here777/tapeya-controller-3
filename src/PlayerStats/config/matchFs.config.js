import { createPlayerStatsConfig } from "./shared.config.js";

/**
 * MatchFS — full-screen batsman match graphic.
 * Same shell as NameFS.
 */
export const matchFsConfig = createPlayerStatsConfig({
  id: "match-fs",
  role: "batsman",
});
