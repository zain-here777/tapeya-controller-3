import { createPlayerStatsConfig } from "./shared.config.js";

/**
 * NameLT — batsman name lower third.
 * LastWicket shell dimensions; name + role left, team logo right.
 */
export const nameLtConfig = createPlayerStatsConfig({
  id: "name-lt",
  role: "batsman",
});
