import { createPlayerStatsConfig } from "./shared.config.js";

/**
 * NameFS — full-screen batsman name graphic.
 */
export const nameFsConfig = createPlayerStatsConfig({
  id: "name-fs",
  role: "batsman",
});
