import { createPlayerStatsConfig } from "./shared.config.js";

/**
 * MatchLT — batsman match lower third.
 * Same UI shell as LastWicket.
 */
export const matchLtConfig = createPlayerStatsConfig({
  id: "match-lt",
  role: "batsman",
});
