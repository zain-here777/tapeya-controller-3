import { createPlayerStatsConfig } from "./shared.config.js";

/** Bowler TournamentFS config. */
export const bowlerTournamentFsConfig = createPlayerStatsConfig({
  id: "bowler-tournament-fs",
  role: "bowler",
});
