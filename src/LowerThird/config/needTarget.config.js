import { createLowerThirdConfig } from "./shared.config.js";

/**
 * NeedTarget — isolated config.
 * Same layout defaults as Default; do not import other controller configs.
 */
export const needTargetConfig = createLowerThirdConfig({
  id: "need-target",
});
