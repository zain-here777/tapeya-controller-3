import { createLowerThirdConfig } from "./shared.config.js";

/**
 * FollowTapeya — isolated config.
 * Same UI as IntroLT; do not import other controller configs.
 */
export const followTapeyaConfig = createLowerThirdConfig({
  id: "follow-tapeya",
});
