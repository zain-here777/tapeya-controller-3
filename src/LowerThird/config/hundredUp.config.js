import { createLowerThirdConfig } from "./shared.config.js";

/**
 * 100Up — isolated config.
 * Same action-banner UI as Four; do not import other controller configs.
 */
export const hundredUpConfig = createLowerThirdConfig({
  id: "100-up",
});
