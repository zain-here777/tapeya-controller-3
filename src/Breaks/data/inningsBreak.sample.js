import { breakMatchBase } from "./matchBase.sample.js";

/** Sample data for the InningsBreak controller only. */
const inningsBreakSample = {
  ...breakMatchBase,
};

export default inningsBreakSample;
