import { breakMatchBase } from "./matchBase.sample.js";

/** Sample data for the StrategicTimeout controller only. */
const strategicTimeoutSample = {
  ...breakMatchBase,
};

export default strategicTimeoutSample;
