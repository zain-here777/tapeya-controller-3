import { breakMatchBase } from "./matchBase.sample.js";

/** Sample data for the RainStopped controller only. */
const rainStoppedSample = {
  ...breakMatchBase,
};

export default rainStoppedSample;
