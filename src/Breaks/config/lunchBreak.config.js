import { createBreakConfig } from "./shared.config.js";

/** LunchBreak — isolated config. */
export const lunchBreakConfig = createBreakConfig({
  id: "lunch-break",
  statusLabel: "LUNCH BREAK",
});
