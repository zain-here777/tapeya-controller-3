import { createBreakConfig } from "./shared.config.js";

/** RainStopped — isolated config. */
export const rainStoppedConfig = createBreakConfig({
  id: "rain-stopped",
  statusLabel: "RAIN STOPPED PLAY",
});
